#rewrite

#>Imports
import async
import discord

#>Client Commands
class clientCommands():
    def _init_:
        self.client = client
        asyncio.get_event_loop().create_task(client.wait_until_ready())
    def keep_alive():
        keep_alive()
